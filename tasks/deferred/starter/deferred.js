// Implement a Deferred: an object that exposes a promise plus the two functions
// that settle it. `then` and `catch` must chain like a native promise.
//
// Do not change the export shape — the tests import exactly this.
export class Deferred {
  constructor() {
    throw new Error('not implemented');
  }
}
